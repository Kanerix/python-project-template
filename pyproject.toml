[tool.poetry]
name = "package"
version = "0.1.0"
description = ""
authors = ["Kasper <dkkasjoe@hotmail.com>"]
readme = "README.md"
package-mode = false                        # set to `true` if project is a package

[tool.poetry.dependencies]
python = "^3.11"
polars = "^1.10.0"

[tool.poetry.group.dev.dependencies]
ipykernel = "*"
ruff = "*"

[tool.poetry.group.test.dependencies]
pytest = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
